{
  "name": "blizzardts-starter",
  "version": "1.0.0",
  "description": "Starter template for BlizzardTS applications",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "blizzardts": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.0.0"
  }
}
